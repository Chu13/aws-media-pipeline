{
  "name": "aws-media-pipeline",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "infra",
    "lambdas/*",
    "web"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-sdk-client-mock": "^4.1.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
